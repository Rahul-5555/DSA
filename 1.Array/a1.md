Perfect bro! 🔥
Hum **one-by-one** har question ko solve karenge —
**pehle explanation (simple language) → fir diagram → fir dry run → fir code**.
Aur last me main tumhe **quiz bhi dunga** to check mastery.
Aaj ka topic: **Array Question 1 — Two Sum**

---

# ✅ **Question 1: Two Sum**

**Given:** nums array + target
**Find:** do indexes jinke numbers ka sum = target

---

# ⭐ Step 1 — Concept (Simple Language)

Array me hume do aise number dhundhne hain jinhe add karne par target mil jaye.

### ❌ Brute Force

Har pair check karo → O(n²)

### ✅ Optimal with Map

Map me hum:

* number ke **complement** ko check karte
* number ka **index store** karte

Formula:

```
complement = target - currentNumber
```

Agar complement map me exist kare → answer mil gaya.

---

# ⭐ Step 2 — Diagram (Memory Map Diagram)

Array: `[2, 7, 11, 15]`
Target: `9`

| i | nums[i] | complement = 9 - nums[i] | Map (value → index) | Match?                       |
| - | ------- | ------------------------ | ------------------- | ---------------------------- |
| 0 | 2       | 7                        | {2: 0}              | ❌                            |
| 1 | 7       | 2                        | {2: 0, 7: 1}        | ✅ 2 mil gaya → indexes (0,1) |

---

# ⭐ Step 3 — Dry Run (Step-by-step)

### Start:

Map = {}

### i = 0

nums[0] = 2
Complement = 9 - 2 = **7**
7 map me nahi →
Map → {2:0}

### i = 1

nums[1] = 7
Complement = 9 - 7 = **2**
2 map me mil gaya → index = 0
So answer = **[0,1]**

---

# ⭐ Step 4 — Code (JS)

```js
function twoSum(nums, target) {
  const map = new Map();

  for (let i = 0; i < nums.length; i++) {
    let complement = target - nums[i];

    if (map.has(complement)) {
      return [map.get(complement), i];
    }
    map.set(nums[i], i);
  }
}
```

---

# ⭐ Ab tumhari practice (quiz-style)

**Explain in your own words:**
👉 *Map use karne ka sabse bada fayda kya hai Two Sum problem me?*

(Short me answer do, galat hua to main correct kar dunga.)



Bhai answer **sahi direction me** hai, but thoda **incomplete** hai.
Main tumhe interview-level perfect version me correct kar deta hoon:

---

# ✅ **Corrected Answer (Simple + Perfect Interview Style)**

Map use karne ka sabse bada fayda ye hai ki:

**👉 Map me search (has) O(1) time me hota hai.**
Iska matlab har element ka complement turant check ho jata hai bina pura array loop kiye.

Isliye brute force O(n²) se
**map approach O(n)** ban jati hai.

Aur isse time complexity **dramatically fast** ho jati hai — jo actual reason hai map use karne ka.

---

# ⭐ Perfect line jo tum interview me bol sakte ho:

**“We use Map because it allows O(1) lookup for the complement, which reduces the overall time complexity from O(n²) to O(n).”**

---

Acha bro, ab **next question** start karein?

### 🔥 **Question 2 — Best Time to Buy & Sell Stock (Buy low → sell high)**

Start karein?
